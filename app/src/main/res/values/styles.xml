<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/TzActionBar</item>

        <item name="android:windowBackground">@drawable/bg1</item>


        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/TzActionBar</item>
    </style>

    <style name="LoginRegisterTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/front1</item>
    </style>

    <style name="TzActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/action_bar</item>

        <!-- Support library compatibility -->
        <item name="background">@color/action_bar</item>
    </style>
</resources>
